# 🤖 AI Agent Rules — Chrlit Studio

> Tento soubor musí být přečten KAŽDÝM AI asistentem na začátku každé session.

---

## ⚠️ POVINNÉ PO KAŽDÉ ZMĚNĚ KÓDU

Kdykoli uděláš změnu v kódu, **MUSÍŠ** aktualizovat relevantní dokumentaci:

### Rychlý checklist

```
[ ] Přidán/změněn soubor v instagram/    → AI_AGENT_KNOWLEDGE_BASE.md §2, §3, §9 + SYSTEM_KNOWLEDGE_BASE.md §7
[ ] Přidán API endpoint                  → SYSTEM_KNOWLEDGE_BASE.md §3, §7 (API Routes tabulka)
[ ] Změněn AI model                      → SYSTEM_KNOWLEDGE_BASE.md §5 + AI_AGENT_KB §7
[ ] Přidána DB tabulka/sloupec           → SYSTEM_KNOWLEDGE_BASE.md §6 + AI_AGENT_KB §6
[ ] Změněn agent pipeline                → obě docs sekce o pipeline
[ ] Nový feedback loop                   → SYSTEM_KNOWLEDGE_BASE.md §4
[ ] Nové gotcha/bug pattern              → AI_AGENT_KNOWLEDGE_BASE.md §8
[ ] Změněn env var                       → SYSTEM_KNOWLEDGE_BASE.md §9
[ ] Změněny LOC nebo file counts         → README.md + AI_AGENT_KB §9
```

---

## 📋 Mapa: Kde co dokumentovat

| Co se změnilo | Soubor | Sekce |
|---------------|--------|-------|
| Architektura, stack | SYSTEM_KNOWLEDGE_BASE | §1 |
| Multi-tenancy | SYSTEM_KNOWLEDGE_BASE | §2 |
| Generovací pipeline | SYSTEM_KNOWLEDGE_BASE | §3 + AI_AGENT_KB §3 |
| Feedback loops | SYSTEM_KNOWLEDGE_BASE | §4 + AI_AGENT_KB §4 |
| AI modely | SYSTEM_KNOWLEDGE_BASE | §5 + AI_AGENT_KB §7 |
| DB tabulky | SYSTEM_KNOWLEDGE_BASE | §6 + AI_AGENT_KB §6 |
| Klíčové soubory | SYSTEM_KNOWLEDGE_BASE | §7 |
| Security, auth | SYSTEM_KNOWLEDGE_BASE | §8 |
| ENV proměnné | SYSTEM_KNOWLEDGE_BASE | §9 |
| Agenti, role | AI_AGENT_KNOWLEDGE_BASE | §2 |
| Záludnosti, gotchas | AI_AGENT_KNOWLEDGE_BASE | §8 |
| Adresářová struktura | AI_AGENT_KNOWLEDGE_BASE | §9 + README.md |

---

## 🏗️ Architektonická pravidla (nikdy neporušovat)

1. **Tenant isolation** — každý `ig_*` dotaz musí filtrovat `client_id`
2. **Config v DB** — žádné config soubory v kódu, pouze `configs/types.ts` + `configs/index.ts`
3. **Config validace** — `loadConfig()` volá `validateConfig()` — nový field musí mít default
4. **Retry logika** — pouze z `utils/retry.ts`, nekopírovat
5. **Admin Supabase** — backend používá `supabase/admin`, nikdy `supabase/client`
6. **Feedback loop** — nový datový zdroj → přidat `performance_score` + weighted selection
7. **Auth guard** — každý nový API route musí mít `requireAuth()` (kromě webhooků)
8. **Rate limiting** — nové generovací endpointy → zvážit přidání rate limitu
9. **Deprecated modely** — `gemini-2.0-flash`, `gemini-3-pro-preview` (404 18.6.2026), `gemini-3.1-pro-preview`, `imagen-4.0-ultra`, `gemini-3-pro-image-preview`, `gemini-3.1-flash-image-preview` (shutdown 25.6.2026) → NEPOUŽÍVAT. Pro tier = alias `gemini-pro-latest` (nepinovat preview ID)
10. **Model ID** — vždy přes `getModel()` z `instagram/models.ts`, nikdy hardcoded string

---

## 📅 Historie verzí docs

| Datum | Verze | Co se změnilo |
|-------|-------|---------------|
| 2026-02-27 | v2.0 | Základní architektura, multi-tenant, retry logika |
| 2026-05-14 | v3.0 | 2-step API, feedback loops, weighted selection, Memory Agent, model upgrade |
| 2026-05-14 | v3.1 | Visual Memory, analyzeVisualPatterns(), Art Director injection |
| 2026-06-02 | v4.0 | **Beta Launch:** rate limiting (10/h), auth na všech routes, config validace, editorial log UI, imageInstructions UI, mock platby, error recovery, onboarding timeout, model upgrade gemini-3.5-flash, 16 DB tabulek |
| 2026-06-10 | v4.1 | **Production Hardening:** tenant isolation (requireProjectAccess/requireClientAccess všude, žádné tenant fallbacky), oprava mrtvého learning triggeru (delta bug), kredit charge při create-job + refund + idempotence, config cache TTL 60s, stuck-job reaper, Sentry + env validace, mock-payment kill switch, link_type (revize vs A/B varianty), reviseCaption() v enginu, smazán /api/ig-generate, dekompozice typů (lib/types/database.ts jako zdroj pravdy) |

| 2026-06-11 | v4.2 | **Native Design Engine:** centrální model registry (`instagram/models.ts`, env overrides `GEMINI_MODEL_*`), migrace image modelů na GA ID (preview shutdown 25.6.), AI Designer (`gemini-3.1-pro`) generuje design briefy, Nano Banana Pro renderuje celý post vč. české typografie + loga (vision QA + korektivní edit + Satori fallback), `ClientConfig.visualEngine`/`videoTier`, `ig_posts.design_brief`, `ig_generation_log.qa_status`, Veo tiers (lite/fast/premium), TTS fallback + audio tags |

| 2026-06-12 | v4.3 | **Landing storytelling redesign** (`app/page.tsx`): copy + přeuspořádání sekcí pro ne-marketingové publikum (hero „Zadáte svůj web. Máte hotový Instagram."), nová Problem sekce, how-it-works posunuto nahoru, showcase posty z reálných oborů (`industry` chip), FAQ přeřazeno + oprava zastaralých „30 kreditů"/„7 dní zdarma" → 15/40/100 kreditů, „3 posty zdarma". Žádné změny logiky, API ani DB |
| 2026-06-14 | v4.4 | **Proof-led landing + reference brands:** hero přepsán na „web → hotový post" + sekce SeedToFlower (Sémě/Květ života vizualizace, `components/SeedToFlower.tsx`). Nová **reference** pipeline: `app/onboarding/core.ts` = auth-free dvojče onboarding logiky (buildManualAnalysisCore/generateConfigCore/saveConfigCore) sdílené headless skripty; `scripts/seed-reference-clients.ts` (4 fiktivní demo značky → config + bucket) + `scripts/export-references.ts` → `lib/reference-data.ts`; `components/References.tsx` sekce; hero/showcase čte reálné vygenerované posty (fallback na placeholdery). `ClientConfig.isReference` flag. **Oprava CLI:** `instagram/cli.ts` předává `configName` do `generateBatch`/`generateOnePost` (jinak `ensureConfig` házel „chybí configName"). Bez DB schema změn (jen nový volitelný config field) |

| 2026-06-15 | v4.5 | **Value-oriented landing restructure** (`app/page.tsx`): 10 sekcí → hodnotový oblouk (~7), důraz na výsledek místo mechaniky. Smazáno „how-it-works" (3 kroky), „30 hodin vs 15 minut" tabulka a credit-cost grid v ceníku; FAQ 10 → 4 (jen nákupní námitky). Nový `components/HeroPlayground.tsx` — hratelný hero (klik na obor → post se poskládá: skeleton → fade obrázku → typewriter captionu; čistá front-end simulace, bez backendu/kreditů, čte `SHOWCASE_POSTS`). Showcase sloučen do `References` galerie — přepsána z husté mřížky na **horizontální slider per značka** (uniform-height karty `h-auto w-auto` = mixed poměry 1:1/4:5/3:4/9:16 **bez ořezu**, šipky + scroll-snap), modal `object-contain` = celý obrázek; `SeedToFlower` zbaven „výukového" textu → úsporná emoční slova; `WaitlistForm` rounded-2xl/xl → rounded-sm. Nová sekce „stakes + cenová kotva" (loss aversion + agentura 15–20k vs od 490 Kč). Bez změn logiky, API ani DB |

| 2026-06-15 | v4.6 | **Agent Psycholog (prompt-vrstva):** nový `instagram/psychologist.ts` — deterministická vrstva prodejní psychologie (`buildPsychologistSection`) vkládaná do copywriterova mega promptu (`buildMegaPrompt` v `caption-generator.ts`), persuazní páky laděné podle `persona.ctaStyle` (hard/soft/medium). **Žádné AI volání ani nový pipeline krok** = bez latence/nákladu/failure-mode. Honesty guardrail (zákaz fake urgence/recenzí/čísel). Nový `ClientConfig.psychologist` (default `true` ve `validateConfig`). Živý review-agent (AI přepíše hotový caption) zaparkován v `brain/Ideas.md`. Bez DB změn |

| 2026-06-15 | v4.7 | **Pravidla věrnosti fotkám (Art Director):** nový `instagram/photo-fidelity.ts` — deterministický blok pravidel odvozený z `BrandImage.tags` (reálné místo/produkt vs generické) vkládaný do briefu Art Directora (`generateDesignBrief` v `image-pipeline.ts`). Brání klamavým hybridům (reálné zahradní židle + zbytek vymyšlený = falešný obraz reálného místa). Aktivuje se jen u značek s reálnými fotkami místa/produktu, jinak `""`. Žádné AI volání. Hlubší enforcement (vázat pravidla na konkrétní fotky přiložené k postu v orchestrátoru) → `brain/Ideas.md`. Bez DB změn |

| 2026-06-15 | v4.8 | **Doporučený styl komunikace na klienta (onboarding):** na konci generování configu (UI path `generateConfigPreview` v `app/onboarding/actions.ts` **i** twin `generateConfigCore` v `core.ts` — pozor, logika je v obou duplikovaná) se navíc vytvoří na míru doporučený styl komunikace (`headline`/`rationale`/`dos`/`donts`) — best-effort try/catch jako u person, takže selhání nerozbije onboarding. Zobrazeno jako **read-only panel** v review kroku (`app/onboarding/page.tsx`, mimo approval state machine) a uloženo do configu (`saveConfigCore` perzistuje celý config JSONB). Nový `ClientConfig.communicationStyle`. Bez DB schema změn |
| 2026-06-15 | v4.9 | **Self-serve obnova hesla:** nové routes `/forgot-password` (action `resetPasswordForEmail`, redirectTo `/auth/callback?next=/reset-password`) a `/reset-password` (action `updateUser({ password })`, vyžaduje recovery relaci). `/auth/callback` zpevněn — `next` sanitizován proti open-redirectu, vypršelý recovery odkaz → `/forgot-password?error=link_expired`. Odkaz „Zapomněl jsi heslo?" na `/login`. Admin nástroj `scripts/reset-password.ts <email> <heslo>` (přímý set hesla přes service role pro zaseknuté účty). Bez DB schema změn (čistě Supabase Auth) |
| 2026-06-15 | v5.0 | **Oprava tenant leaku v onboarding gate:** `checkOnboardingStatus()` měl „fallback", který nový účet bez `user_clients` linku přilepil k **prvnímu aktivnímu klientovi v DB** (role `member`) a vrátil `needsOnboarding:false` → nový uživatel viděl cizí značku místo onboardingu. Fallback odstraněn — členství v `user_clients` je jediný ownership signál (`clients.user_id` se neplní). Chybějící link = onboarding. `role='member'` = bug artefakt (nikde jinde nevzniká); čisticí skript `scripts/cleanup-orphan-links.ts` (dry-run / `--fix` / per-email). + **Admin menu gating:** `isCurrentUserSuperAdmin()` server action gateuje admin nav skupinu (Onboarding/Waitlist/Products) v `AdminSidebar` — dřív viditelná všem. Bez DB schema změn |
| 2026-06-15 | v5.1 | **Oprava onboarding slug-kolize → posty do cizího klienta:** `insertClient` (obě kopie: `core.ts` export + `actions.ts` private) při kolizi slugu vytvoří `-xxxx` suffix, ale vracel jen UUID → `saveReviewedConfig`/`saveConfigCore` vrátily původní nesuffixovaný slug → `generateMonthlyPlan`/`generateShowcasePost` generovaly do již existujícího (cizího) klienta, uživatelův nový klient zůstal prázdný. Fix: `insertClient` vrací `{id, slug}`, oba save twiny vrací reálný slug. Navíc `generateShowcasePost` dostal `requireProjectAccess()` (měl jen `requireAuth()`) — bránil cross-tenant zápisu. Bez DB schema změn |
| 2026-06-15 | v5.2 | **Oprava slug-vs-UUID na subscription/payment API:** `projectId` (StudioContext) = slug, ale `/api/payments/create` (klient posílal `clientId: projectId`) hledal `clients.id=<slug>` → „Client not found" při kliku na „Přejít na {plán}"; `/api/subscription` hledal `subscriptions.client_id=<slug>` → tiše null → žádný plán i s aktivním trialem. Fix: `SubscriptionSection` + `PaywallProvider` posílají `clientSlug: projectId`; `/api/subscription` resolvuje identifikátor přes `requireProjectAccess()` (slug→UUID + membership check). Bez DB schema změn |
| 2026-06-15 | v5.3 | **Free plány aktivují bez platby:** klik na bezplatný plán (Beta Trial, `price_czk=0`) šel taky přes `/api/payments/create` → „Missing COMGATE_MERCHANT_ID..." (Comgate creds nejsou v beta env). Nová server action `activateFreePlan(projectSlug, planId)` v `settings-actions.ts` — `requireProjectAccess` + guard `price_czk===0` (placené plány odmítne) + `activatePaidPlan()`. `SubscriptionSection.handleUpgrade` větví: `price_czk===0` → `activateFreePlan` + `refreshSubscription`, jinak Comgate. Placené plány stále vyžadují Comgate env (`COMGATE_MERCHANT_ID/SECRET`, `COMGATE_TEST=true` pro test gateway; mock je na produkci vypnutý killswitchem). Bez DB schema změn |

| 2026-06-16 | v5.4 | **Oprava „Native engine failed" (model 404):** `designer` model `gemini-3.1-pro` vrací 404 (není dostupný na v1beta / API klíči) a `generateText` fallbackoval jen na 503/429 → 404 shodilo celý native pipeline. Fix: `MODELS.designer` → `gemini-3.5-flash` (fallback `gemini-2.5-flash-lite`; Pro model lze vrátit přes `GEMINI_MODEL_DESIGNER` env override), `generateText` fallbackuje i na 404/`not found`. Image/video fallbacky 404 zatím neřeší. Bez DB schema změn |

| 2026-06-16 | v5.5 | **Best-quality model audit (ověřeno přes `ai.models.list()` + reálné volání):** opraveno několik neexistujících model ID, která by 404ovala. `designer` → `gemini-pro-latest` (best Pro, alias; `gemini-3.1-pro` neexistuje, `gemini-3-pro-preview` už je mrtvý, `gemini-3.1-pro-preview` deprecated 25.6.); video tiers → `veo-3.1-{lite,fast,}-generate-preview` (původní `-001`/`veo-3.1-lite` neexistovaly → reels by 404ovaly); `tts` fallback → `gemini-2.5-flash-preview-tts`. Image už byl optimální (`gemini-3-pro-image` GA). `generateText` má nový `fallbackModel` (designer fallback = `gemini-2.5-pro`, ne text flash-lite). `text`/`vision` zůstávají `gemini-3.5-flash` (rychlost + 300s timeout; Pro pro všechny text-agenty by riskoval timeout). Pro nejlepší reels: `ClientConfig.videoTier="premium"` (default `fast`). Bez DB schema změn |

| 2026-06-16 | v5.6 | **Designer 503/deadline → fast fallback (ne overlay):** Pro designer (`gemini-pro-latest`) občas vrací 503 „Deadline expired" (throttling Pro modelu na free Gemini tieru / overload). `withRetry` ho zkouší 3×, pak `generateText` fallbackoval na `gemini-2.5-pro` = taky pomalý Pro → taky timeout → `renderImage` spadl na legacy overlay engine (degradace vizuálu). Fix: designer fallback `gemini-2.5-pro` → `gemini-3.5-flash` (rychlý, dokončí do deadline → native Nano-Banana render pokračuje). **Skutečný fix kapacity = zapnout placený (pay-as-you-go) tier na Gemini API** (Google AI Studio/Cloud projektu za `GEMINI_API_KEY`) — free tier Pro modely silně throttluje. Není to Comgate subscription appky. Bez DB schema změn |

| 2026-06-16 | v5.7 | **Designer primary `gemini-pro-latest` → `gemini-2.5-pro`:** i na placeném Gemini tieru `gemini-pro-latest` (alias na nejnovější Pro) občas vrací 503 „Deadline expired" (transient congestion nejžhavějšího modelu). `gemini-2.5-pro` = zralý GA Pro s větší kapacitou/nižší latencí → spolehlivější, stále Pro kvalita (volba uživatele). Fallback zůstává FAST `gemini-3.5-flash`. Override `GEMINI_MODEL_DESIGNER` pořád možný. Bez DB schema změn |

| 2026-06-17 | v6.3 | **Fix „lazy web" regrese z v6.1 — split `text` (fast) vs `textPro` (in-job).** v6.1 dalo CELÝ sdílený `text` na `gemini-3-pro-preview` → všechny INTERAKTIVNÍ cesty (content-plan preview, který dělá několik sekvenčních volání, onboarding, produkty) zpomalily / „Obsahový plán" se točil donekonečna (pomalý Pro preview model × multi-call). Fix: `MODELS.text` zpět na FAST `gemini-3.5-flash` (fallback `gemini-2.5-flash`) — UI responzivní. Nový `MODELS.textPro` = `gemini-3-pro-preview` (fb `gemini-3.5-flash`) jen pro **copywriter** (caption v `caption-generator.ts`, běží v generation jobu s 800s budgetem → latence neovlivní browsing); copywriter call dostal model-fallback smyčku (Pro 503 → flash, ne throw). Critic zůstává na `text` (flash, scorer). `designer` + `visionQA` zůstávají gen-3 Pro (taky in-job, mají fallback). Image kvalita (designer brief + `gemini-3-pro-image` + QA) stále plně gen-3 Pro. Bez DB schema změn |
| 2026-06-17 | v6.2 | **Vision QA judge → gen-3 Pro (#2 leverage point).** `verifyNativeImage` (QA gate native enginu — běží na KAŽDÉM nativním renderu, až 2× per obrázek: init + po korekci) běžel na sdíleném `vision` = `gemini-3.5-flash`. Nový dedikovaný `MODELS.visionQA` = **`gemini-3-pro-preview`** (fallback `gemini-3.5-flash`) → silnější judge zachytí jemné CZ typo/logo defekty, které flash mine (image generation už je na top `gemini-3-pro-image`, QA byl nejslabší článek na vizuální straně). Sdílený `vision` zůstává flash (tagging/logo-detect/overlay review = high-volume/levné). `verifyNativeImage` přepsán na model-fallback smyčku (`visionQA` primary → fallback → teprve pak fail-open), aby Pro 503 QA netiše nepřeskočil. Override `GEMINI_MODEL_VISIONQA`. Bez DB schema změn |
| 2026-06-17 | v6.1 | **Brain upgrade gen-2.5 → gen-3 Pro (80/20).** `MODELS.text` + `MODELS.designer` primary `gemini-2.5-pro` → **`gemini-3-pro-preview`** (potvrzeně živý na klíči, NENÍ deprecated `gemini-3.1-pro-preview`; gen-3 Pro výrazně silnější než starý gen-2.5 Pro). V logu Google API bylo vidět „nízký" model, protože brain běžel na gen-2.5 Pro a/nebo padal na novější `gemini-3.5-flash` fallback. Jedna změna zvedne kvalitu copywritera, critica, editorial board i design briefů (= ~90 % výstupu); image už byl na top `gemini-3-pro-image`, vision/tts/video zůstávají levné (správně v 80 % bez dopadu). Bezpečné teď, když `maxDuration=800` (v6.0) dá Pro modelu čas doběhnout; `generateText` fallbackuje na 503/429/404. Override `GEMINI_MODEL_TEXT` / `GEMINI_MODEL_DESIGNER` stále možný. Bez DB schema změn |
| 2026-06-17 | v5.9 | **Loop-integrity fixes (recency decay + exploration + rejection learning).** Smyčka metrik→učení byla uzavřená, ale jen přes ruční zadání metrik (`MetricsInputForm` → `updateIGPostMetrics()` → `analyzeAndLearn`/`propagateMetricsToSources`); chybí jen automatický IG Graph API import (plánovaný drop-in do `updateIGPostMetrics()`, cílová surface = Instagram API with Instagram Login). Tři interní vylepšení smyčky bez Meta závislosti: (1) **`decayedScore()`** (`service.ts`) — `performance_score` slábne s ~120denním poločasem (decay podle `last_used_at`/`used_at`), aplikováno v `getWeightedIdeas` + `getWeightedReviews`; staré hity přestanou dominovat výběr. (2) **Exploration boost** — nezměřené zdroje (`!times_used_with_metrics`) dostanou garantovaně 2x váhu (optimism under uncertainty) místo zahrabání mezi prokazatelně slabé. (3) **`learnFromRevision()`** (`memory-agent.ts`) — `revisePost()` spustí (fire-&-forget) extrakci `avoid`/`preference` memory z feedbacku uživatele (nejsilnější signál); stejný dedup/confidence pattern jako `learnFromVariantSelection`. Bez DB schema změn |
| 2026-06-17 | v6.0 | **Vercel Pro → `maxDuration` 300 → 800.** Zakoupen Vercel Pro plán (Fluid Compute) → strop funkcí zvednut z 300s na 800s. Bumpnuto na těžkých AI cestách: `ig-run-job` (hlavní generation pipeline), `onboarding/layout` (web + IG scraping + config gen), `(dashboard)/layout`, `cron/growth-snapshot`. Rychlé routy beze změny (`ig-create-job`=10s, `ig-job-status`=5s, `ig-learn`=60s). Stuck-job reaper v `ig-job-status` zvednut z 8 na **15 min** (musí přesáhnout run-job 800s, jinak by falešně reapnul živý-ale-pomalý job). Tím padá riziko all-Pro timeoutu z v5.8. Bez DB schema změn |
| 2026-06-16 | v5.8 | **Brand-specific post formáty + all-text-Pro.** (1) `MODELS.text` → `gemini-2.5-pro` (fallback `gemini-3.5-flash`) = všichni text agenti (copywriter, critic, editorial, context, ideas, content-plan, onboarding) na Pro. `maxDuration` zůstává **300** (Vercel plán zatím bez raised limitu — `maxDuration=800` shodí deploy; bump až plán dovolí >300s; **pozor: all-Pro tak může na 300s timeoutovat na složitých postech s editorial board**), stuck-job reaper 8 min. (2) Custom formáty: nový `ClientConfig.postTypeDefs` + `generateCustomFormats()` v `core.ts` (sdílené oběma onboarding twiny, best-effort) generuje brand-specific formáty s popisy; `ensurePostTypes()` rozšířen, aby je perzistoval do `ig_post_types` (volá se v save path). `getIGPostTypes` scopnut per `client_id` (fallback na generický set jen když klient nemá řádky). GenerateTab ukazuje popis formátu. Review detekce v autopilotu dle name patternu. `scripts/backfill-post-types.ts` pro existující klienty. Bez DB schema změn (sloupce `description`/`uses_product` už existovaly) |

| 2026-06-16 | v5.9 | **Reference fotky se reálně používají (label dle tagů, ne industry regexu):** `loadReferenceImages` v `image-orchestrator.ts` labeloval VŠECHNY brand fotky jedním labelem odvozeným z `isLocation/isProduct` industry regexu (míjel třeba květinářství → generický „maintain style" label → Nano Banana fotky bral jen jako volnou inspiraci). Nově `brandRefLabel(tags)` dává label per fotku dle jejích tagů: product/food/… → „EXACT product — reproduce faithfully", interior/exterior/shop/… → „real location — use this exact place", jinak style. Labely se posílají modelu (`generateImageWithReferences` je interleavuje jako text party). Data byla OK (klient měl 8 fetchnutelných fotek); problém byl slabý label. Bez DB schema změn |

| 2026-06-16 | v5.10 | **Viditelné + opravitelné AI štítky fotek.** Auto-tagging (`tagBrandImage`, controlled vocab `VALID_TAGS`) už běžel na uploadu, rescanu i onboarding scrapu — ale štítky byly v UI neviditelné (`getBrandImages` vracel jen URL). Nově: `getBrandImageObjects()` vrací `BrandImage[]` (url+tags+description); BrandTab zobrazuje štítky pod každou fotkou (+ „bez štítku" warning) a má tlačítko **Přeznačit AI** → `retagBrandImages(slug)` stáhne a znovu oštítkuje všechny fotky klienta (pro legacy/neoznačené). Štítky pohání referenční labely (v5.9) i photo-fidelity. Bez DB schema změn |

| 2026-06-16 | v5.11 | **Normalizace nahraných fotek (PC i mobil).** `uploadBrandImage` ukládalo raw soubor bez úprav → telefonní fotky (rotace z EXIF, >10 MB, raw iPhone HEIC) se nezobrazovaly / nešly otagovat / nešly použít jako reference. Nově každý upload projde `sharp`: auto-rotate (EXIF), resize max 2048 (fit inside), re-encode JPEG q85; limit zvednut 10→25 MB; ukládá se vždy jako `.jpg` (`image/jpeg`) a tak se i taguje. sharp 0.34 dekóduje AVIF, ale NE HEIC (HEVC licensing) — raw HEIC (vzácné, iOS Safari ho při web uploadu obvykle převede na JPEG sám) → jasná chybová hláška s návodem. Bez nové závislosti. Bez DB schema změn |
| 2026-06-24 | v6.4 | **Manuální „handoff" publikování (BEZ App Review):** nové tlačítko **📲 Publikovat na Instagram** v `PostsTab` (karta + detail) → `PublishHandoffModal.tsx`. Na telefonu Web Share API (`navigator.share({ files })`) pošle obrázek(y) přímo do IG appky + caption do schránky (IG share text nepřebírá → schránka je most); desktop fallback kopírovat/uložit; po sdílení „✓ Označit jako publikováno". Funguje hned, pro libovolný účet **i reels** (na rozdíl od cron auto-publishe čekajícího na 2. App Review). Uživatelský návod `docs/POSTING_GUIDE.md`. Bez DB/API/model změn (klientský UI nad existujícími veřejnými URL obrázků). |
| 2026-06-25 | v6.5 | **Auto metriky → learning (roadmap step 3):** nový `instagram/metrics-sync.ts` — `syncPostMetrics(clientId)` stáhne IG insights (`fetchMetrics` v `lib/channels/instagram.ts`: `like_count`/`comments_count` + `/insights?metric=reach,saved,shares,profile_visits`, defenzivně per-metric), spáruje je s našimi `posted` posty (**caption-match backfill `ig_media_id`** pro handoff posty bez media id) a nakrmí **stávající** learning kaskádu. Posvátná smyčka vyčleněna do session-less `writeIGPostMetrics` + `fireMetricsLearning` (sdílí UI i cron; fire-once po syncu místo per-post — `analyzeAndLearn` je AI volání). `updateIGPostMetrics` teď jen guard + delegace. Spouštěče: denní cron `/api/cron/ig-metrics-sync` (`0 7 * * *`) + tlačítko „Načíst metriky z Instagramu" v PerformanceTab (`syncMetricsAction`). Funguje hned pro vlastní účet (Standard Access — insights scope už se žádá v OAuth); tenanti po 1. App Review. Manuální `MetricsInputForm` zůstává fallback. Bez DB schema změn (sloupce `ig_media_id`/metriky už existují). |

| 2026-06-25 | v6.6 | **content_publish scope v OAuth (dogfood auto-publish) + setup guide:** `app/api/ig-connect/start` nově žádá i `instagram_business_content_publish` (vedle basic+insights) → vlastní účet (chrlit, app-role) může pod Standard Access **auto-publikovat** (`ig-publisher` cron), ne jen handoff. V dev mode se připojí jen účty s rolí, takže request publish scope je bezpečný; cizí (tenant) účty pořád potřebují content_publish schválený v **2. App Review** před přechodem na Live (jinak drop scope pro non-role). Nový `docs/INSTAGRAM_SETUP_GUIDE.md` (fresh-start setup chrlit účtu od nuly). Bez DB / logiky změn (scope string + docs). |
| 2026-06-25 | v6.7 | **„Publikovat hned" (instant Graph publish) + iOS Zkratka recipe:** handoff modal (`PublishHandoffModal`) nově nabízí **⚡ Publikovat hned** když je účet připojený (image/carousel) — `publishNowAction` (`calendar-actions.ts`) armne post (`status='scheduled'`, `scheduled_for=now`), `ig-publisher` cron ho vydá ≤60s; UI pollne `getPostPublishStatus`. **Žádné synchronní Graph volání** (carousel container-polling by timeoutnul server action) → reuse publisher path, zero risk. Manuální Web Share zůstává sekundární / pro reels / nepřipojené. `PostsTab` předává `connected` (`getConnectionStatus`). Nový `docs/IOS_SHORTCUT_HANDOFF.md` (Zkratka v share sheetu — honest: IG neumožní auto-post ze zkratky, jen příprava). Bez DB schema změn. |

| 2026-07-02 | v6.8 | **AI Consistency Program — cross-family Claude judge (nasazeno na prod):** vícefázový program proti nekonzistenci postů (`docs/AI_PROVIDER_STRATEGY.md`). (1) Deterministické teploty dle role (`TEMPERATURES`/`getTemperature` v `models.ts` — judge 0.25, copywriter 0.75, designer 0.6, creative 0.9; override `GEMINI_TEMP_<ROLE>`) + fixní persona per post (`selectPersonaForPost`, `ContentPillar.targetPersona`). (2) Few-shot brand-voice kotva (`ClientConfig.brandVoiceExamples`, seed z reálného IG). (3) Judge-insight memory (`learnFromCriticInsights` + `upsertMemory` v `memory-agent.ts`; opakující se critic `fix` → `avoid` memory, pillar-scoped `ig_brand_memory.pillar`, migrace `20260701_brand_memory_pillar.sql`). (4) **Cross-family judge:** Critic (`scorePost`) + oba Chief-Editor reviews volají `judgeText()` (`instagram/judge.ts`) → **Claude `claude-sonnet-5`** (`instagram/anthropic-client.ts`) když je `ANTHROPIC_API_KEY`, jinak Gemini `textPro` ladder @ temp 0.25 (writer≠judge). Sonnet 5 = 5-gen (bez `temperature`/`thinking`, `effort:"low"`). Kill switch `CLAUDE_JUDGE=off`. Přidán `@anthropic-ai/sdk`. Env: `ANTHROPIC_API_KEY`, `CLAUDE_JUDGE`, `GEMINI_MODEL_JUDGE`. **Doc fix:** Pro tier (`textPro`/`designer`/`visionQA`) opraven na alias `gemini-pro-latest` (fallback 2. Pro `gemini-2.5-pro`) — `gemini-3-pro-preview` mezitím 404 (18.6.). |

| 2026-07-05 | v7.1 | **Product truth v pipeline + editor formátů.** (1) **Produktová fotka reálně do renderu:** `ig_products.image_urls` byl u klientů prázdný a lokální `instagram/product-images/` není v `outputFileTracingIncludes` → na Vercelu se produktová reference NIKDY nenačetla (Nano Banana si produkt vymýšlel). `scripts/upload-product-images.ts` rozšířen o sync `image_urls` (P0 zdroj, funguje všude) + spuštěn pro hanzfans (21 fotek). Storage lookup (P1) v `loadReferenceImages` zkouší slug I uuid (dashboard upload ukládá pod uuid). (2) **Product-aware native pipeline:** `generateDesignBrief`/`generateCarouselDesignBriefs` dostávají `product` (`ProductBriefInfo`) → designer ví, že existuje EXACT fotka a musí komponovat scénu KOLEM reálného produktu; `buildNativeImagePrompt` má PRODUCT FIDELITY blok; `verifyNativeImage` porovnává render s referenční fotkou (`productAccurate`, mode `require` pro single image / `if-present` pro carousel slidy); product-mismatch → REGENERACE s referencemi (edit fotku nikdy neviděl), ne korektivní edit. Carousel native path nově přikládá produktovou referenci ke KAŽDÉMU slidu (dřív jen logo na coveru). (3) **Editor formátů (SettingsTab „Formáty"):** `upsertPostFormat`/`removePostFormat` v `config-actions.ts` — atomický zápis do všech 4 zdrojů (config.postTypes + postTypeDefs + postFormats + pillar membership + ig_post_types řádek); ADITIVNÍ, nikdy neregeneruje celý set (na rozdíl od `generateCustomFormats`, které při onboardingu/backfillu VŠE nahradí — to je příčina „formáty zmizely"). (4) **`PostTypeDef.manualOnly`:** formát jen pro ruční výběr v Tvorbě — autopilot random selection, `buildSmartWeekPlan` i `planWeek` ho nikdy nevyberou samy (guard pro soutěže/limitky = reálné závazky značky). Pro hanzfans založen formát `soutez_o_merch` (🎁 manualOnly, uses_product, pillar humor → soft CTA = engagement mechanika like+follow+tag bez webu). Bez DB schema změn |

| 2026-07-05 | v7.2 | **Formáty↔pilíře: reconcileFormats (fix orphan bugu + odstranění driftu) + editovatelný overlayStyle.** Formát byl denormalizovaný do 5 míst; smazání pilíře v „Témata" (plain `updateClientConfig` save, bez reconcile) osiřelo jeho formáty — zmizely z auto-plánování a `getPillarForType` je mis-atributoval na první pilíř. (1) **Nová čistá/deterministická/idempotentní `reconcileFormats(config)`** (`instagram/configs/reconcile.ts`): `postTypeDefs` (vč. `.pillar`) = SOURCE OF TRUTH, přebuduje `postTypes` / `postFormats` / pillar membership jako projekci (re-home orphanů: `def.pillar` → aktuální členství → první pilíř; drop dead refs; backfill postFormats bez přepisu existujících; opraví i `def.pillar`). (2) **Zapojení:** `validateConfig` (KAŽDÝ load = self-heal), `updateClientConfig` (durable fix — pilíř save už neosiří), delegace v `upsert`/`removePostFormat` (nahrazuje ruční 4-zdrojový sync). (3) **`audit-formats-pillars.ts --fix`** používá `reconcileFormats` na RAW configu (detekce driftu čte RAW, protože load teď healuje) — nově re-homuje `typesNoPillar` (dřív jen reportoval). (4) **removePillar** předem varuje (kolik formátů + kam se přesunou). (5) **`overlayStyle` volitelný v editoru** (`PostFormatInput` + `FormatFields` select gated dle média): reel vždy `"none"`, statické médium nikdy `"none"` (`resolveOverlayStyle`). Bez DB schema změn |

| 2026-07-06 | v7.3 | **Satori/overlay engine KOMPLETNĚ odstraněn — native-only render + ship-best-native.** Legacy overlay engine se pouštěl jen jako fallback když native neprošel QA a produkoval degradovaný text-stamp (uživatel: „pořád to někdy padá do fallbacku"). Audit: 0 klientů mělo `visualEngine: "overlay"` (všech 13 native), takže overlay byl fakticky mrtvý kód. (1) **Ship-best-native:** `renderImageNative` / `renderCarouselNative` už nevrací null při QA failu — retry (korektivní edit / product-regen) + **1 čerstvá regenerace**, pak publikuje **nejlepší nativní pokus** (`qaScore` v `image-pipeline.ts`, `qa_status "native_forced"`) místo Satori. Null = jen tvrdý infra fail (gen threw) → post bez obrázku. (2) **Smazáno:** `instagram/text-overlay.ts` (683 LOC), `renderImageOverlay`, `renderCarouselOverlay`, `renderCoverViaSatori`, `reviewOverlayComposition`, `refineImagePrompt`, `refineCarouselPrompts`, `ClientConfig.visualEngine`, SettingsTab engine toggle + „Overlay Engine (záloha)" karta. Deps `satori` + `@resvg/resvg-js` a `instagram/fonts/*.ttf` odstraněny (`next.config` serverExternalPackages + fonts tracing). (3) **revisePost** (`variant-actions.ts`) migrován z overlay na native `renderImage` (jen single-image posty; carousel/reel si nechá původní médium). (4) `overlayStyle` zůstává v configu/editoru ale je jen advisory (AI Designer rozhoduje layout); reel-cover fallback je text-free obrázek (ne Satori). Bez DB schema změn |

| 2026-07-06 | v7.4 | **Vlastní fotka = vizuální základ designovaného postu (ne raw publish).** `options.customImageUrl` (upload v GenerateTabu) dřív v `autopilot.ts` obcházel celý visual engine — raw fotka se publikovala 1:1 bez designu/typografie/loga a copywriter ji nikdy neviděl. Nově: (1) **Autopilot krok 0.5:** 1× vision popis fotky (`analyzeImagesWithText`, běží i při checkpoint resume) → injektován do mega promptu (caption drží zadané téma, ale nesmí být s fotkou v rozporu — fotka NENÍ téma) a předán do renderu. (2) **`RenderContext.userPhotoUrl/userPhotoDescription`** → `loadUserPhoto()` v `image-orchestrator.ts` (export, sdílí carousel) attachne fotku jako ref "CLIENT photo — mandatory visual base". (3) **Designer je text-only** → `UserPhotoBriefInfo` + `buildUserPhotoSection` v `generateDesignBrief`/`generateCarouselDesignBriefs` (u carouselu fotka = základ COVERU, inner slidy bez ní); `buildNativeImagePrompt` má CLIENT PHOTO FIDELITY blok (celá fotka nebo záměrný crop, grading/reframe/text OK, výměna scény/ilustrace FORBIDDEN). (4) **QA:** `verifyNativeImage` bere `userPhotoRef` → `photoUsed` (fail → regenerace s refs, edit ji nespraví; v `qaScore` váží +100 jako špatný produkt). (5) Raw-publish větev v autopilotu smazána; u reels se fotka dál ignoruje; GenerateTab má vysvětlující hint. Bez DB schema změn |

| 2026-07-06 | v7.5 | **Nápady: poctivá atribuce + lifecycle UI + Nápověda v sidebaru.** (1) **Fix falešné atribuce nápadů (posvátná smyčka):** když `generateOnePost` dostal explicitní `topic` (plán/kalendář/ruční zadání), Researcher stejně vybral weighted nápad, linkoval `idea_id` a cooldownoval ho — přitom `buildMegaPrompt` nápad při `userTopic` ignoruje (prokazatelně nulový vliv na obsah) → nápad sbíral metriky za cizí post. Nově: explicitní topic **přeskakuje** weighted výběr; nový `options.ideaId` (uživatelem vybraný nápad) jde `GenerateTab` („💡 Vybrat z nápadů" nastaví `selectedIdeaId`, ruční edit tématu ho čistí, chip „Napojeno na nápad") → `ig-create-job` (ownership validace PŘED charge, uloženo v `ig_jobs.config.ideaId`) → `ig-run-job` → `getIdeaById` (client-scoped, miss = throw). Campaign worker bez změny (vždy posílá topic = nově čistý). (2) **IdeasTab lifecycle + čitelnost:** explainer „Zásobník témat", lidské badges (🔥 Funguje / Nové / ×N použito / 💤 Odpočívá / Vypnuto) místo neviditelného `performance_score`, `deleteIdea`/`setIdeaActive` akce (`ig-generate-action.ts`), fix fantomového `times_used` (reálný sloupec = `used_count`; insert fantomové pole zapisoval), pillar badge mapován na label. (3) **Subcategory fix:** pillar-level AI generace ukládala literál `"AI Generated"` → nápady nematchovaly žádný filter chip; nově model přiřazuje `categoryId` (enum ve schématu), legacy hodnoty se v UI normalizují na „Bez kategorie". (4) **Jednotný cooldown:** `getWeightedIdeas` respektuje per-idea `cooldown_days ?? 90` (dřív hardcoded 90d vs. `getAvailableIdeas` per-idea = dva režimy); dashboard `ideasAvailable` počítá stejným pravidlem + action item „Docházejí nápady" (<5 dostupných). (5) **Nápověda:** FaqTab byl kompletní ale NEDOSAŽITELNÝ (žádný nav entry) — přidán „❓ Nápověda" do sidebaru, „▶ Přehrát průvodce" (page-level `useTutorialState` instance přes prop — druhá instance hooku by overlay neotevřela), FAQ odpovědi o nápadech přepsány na zásobník-mentální-model. (6) Hygiena: mrtvý `LogsTab` import pryč, admin sekce (`products`/`onboard`/`waitlist`/`approvals`/`mailing`) gatovány i v renderu `page.tsx` (deep-link hash je jinak obešel), nepoužívaný `onOpenTutorial` prop odstraněn. Bez DB schema změn |

| 2026-07-07 | v7.6 | **Jedna souvislá content pipeline: Zásobník témat ↔ Měsíční plán ↔ Kampaň ↔ Výkon.** Plán a banka nápadů byly úplně odpojené (plán si témata vymýšlel z nuly, onboarding banku neseedoval, kampaně nápady nelinkoval). (1) **Plán čerpá z banky:** `generateContentPlan` volá `getWeightedIdeas(count)` → prompt sekce „ZÁSOBNÍK TÉMAT" (🔥 hint pro ověřené), model vrací `ideaIndex` (optional ve schématu) → **clamp v kódu** (integer, 1..N, každý max 1×; halucinace = položka „vymyšlená") → `ContentPlanItem.ideaId/ideaTitle`; weak-hook retry drží původní ideaIndex, fill passes ho stripují; preview je BEZ side-effectů. (2) **Deposit-back:** `startCampaign` validuje příchozí ideaIds (ownership, browser payload!) a vymyšlená schválená témata insertuje jako nové nápady (explicitní `client_id`, cooldown 30) — POUZE tam (worker při resume nikdy, duplikace). (3) **Threading:** `plan[*].ideaId` → worker → `generateOnePost({ideaId, topic})` = poctivá atribuce (topic z nápadu VZNIKL — nekoliduje s v7.5 skipem); worker před předáním ověří existenci (smazaný nápad = drop atribuce, ne fail). Smyčka: plán→post→metriky→score→příští plán. (4) **Onboarding:** nový `seedIdeaBank` (2 top pilíře × 6, zdarma, non-fatal) běží PŘED prvním plánem; done screen → `#generate` (uživatel přistane v čekajícím plánu); consume effect plán autoDistribuuje (má data); DashboardTab banner „první plán čeká" když localStorage draft nespotřebován; nudge „Docházejí nápady" gated na `ideas>0 || posted>0` (ne day-one). (5) **planWeekAction ODSTRANĚN (billing leak):** generoval posty synchronně BEZ kreditů; CalendarTab „Naplánovat týden" → `generateIntent {plan,1w}`; `instagram/content-planner.ts` smazán. (6) **plan_draft vyřazen:** showcase posty zůstávají `draft` (vstupují do normální approval pipeline; day-one dashboard ukazuje „3 koncepty čekají" místo kritického „žádný obsah"); legacy řádky folded do draft handlingu (stats/list/approve). (7) Kampaňový e-mail: deep-link calendar→**posts**, copy „připraveno ke kontrole" (posty končí `ready`, nic se samo nepublikuje). Bez DB schema změn |

| 2026-07-14 | v7.7 | **Claude vision judge ve skutečnosti NIKDY neběžel (tichý 400 → fallback na Gemini).** Vercel logy: `⚠️ Claude vision judge (vision-qa) failed — falling back to Gemini: 400 … image.source.base64: The ima…`. Příčina: `verifyNativeImage` posílá render natvrdo jako `mimeType: "image/png"`, ale Nano Banana Pro vrací **JPEG** bajty — a Anthropic špatný `media_type` tvrdě odmítne (*"specified using the image/png media type, but the image appears to be a image/jpeg image"*). Mismatch je deterministický → **každé** Claude vision-QA volání 400ovalo a propadlo na Gemini ladder. Fail-open kontrakt fungoval přesně jak měl a tím ten výpadek schoval: posty se generovaly dál, QA běžela dál — jen Gemini hodnotil vlastní render u každého postu, tedy přesně ta self-preference, kvůli které `judgeVision` vznikl. Fix v `instagram/anthropic-client.ts`: nový `toImageBlock` normalizuje obrázky před odesláním — `media_type` se **detekuje z bajtů** (label volajícího se ignoruje) a klampují se zbylé dva tvrdé limity (max 8000 px na hranu; max 10 MB base64 — oba ověřeny proti živému API; přes limit → resize na 2576 px dlouhá hrana + případně JPEG step-down). `img.mimeType` zůstává ve tvaru, protože ho čte Gemini fallback v `judge.ts`. Ověřeno end-to-end: Claude teď přečte i českou diakritiku („PŘÍBĚH") z JPEGu, který dřív skončil hard 400. Pozn.: srovnání Claude vs. Gemini QA tím pádem **začíná až tímto deployem** — dosavadní „unproven" verdikt v `AI_PROVIDER_STRATEGY.md` neměl žádná data, protože Claude větev nikdy neproběhla. Bez DB schema změn |

| 2026-07-16 | v7.8 | **Pricing v5 — přecenění + 4. tier Impérium.** Po přidání funkcí (Claude judge, hlubší pipeline) vzrostly náklady na generování → plány přeceněny (kreditové váhy image 1 · carousel 3 · reel 5 sedí na náklad; extra kredit 15→49 Kč, aby dobíjení nebilo předplatné). Nový model: **Start 990/20 · Růst 1 990/45 · Dominance 3 990/100 · Impérium 7 990/220** (nový kotevní tier pro agentury/e-shopy, `chrlit_imperium`, max_projects 3). Migrace `supabase/migrations/20260716_pricing_v5.sql` (full ON CONFLICT DO UPDATE všech 4 plánů, aplikováno na prod). UI: landing (`app/page.tsx`, 3→4 sloupce), dashboard (`SubscriptionSection` tagline + grid, čte plány dynamicky z `/api/plans`), FAQ, meta description (`layout.tsx`), paywall/unlock CTA (490→990), error hlášky o dobití (15→49 Kč v `lib/subscription.ts`). Interní unit-economics analýza je mimo repo (`docs/pricing/`, gitignored — důvěrné). Bez DB schema změn (jen data v `subscription_plans`) |

*Při dalším updatu přidej řádek sem.*
